{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run build-json && vite", 
    "build": "vite build",                
    "start": "vite preview",              
    "lint": "eslint . --fix",             
    "test": "jest",                       
    "clean": "rimraf dist",         
    "build-json": "node build.js"        
  },
  "dependencies": {
    "vite": "^4.0.0"
  }
}
